#include <vector>
#include <iostream>

using namespace std;

void greet();
void view_grid_position(vector<char> position);
bool checking(vector<char> position, char side);
void winning(int win);
